version: '3.9'

volumes:
  postgres_volume:

services:
  postgres:
    image: postgres:14-alpine
    container_name: fileprocessing-postgres
    env_file:
      - .env
    ports:
      - "54321:5432"
    volumes:
      - postgres_volume:/var/lib/postgresql/data
